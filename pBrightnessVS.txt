#version 300 es

layout (location = 0) in vec4 aPosition;

uniform mat4 uModelMat, uViewMat, uProjMat;

out vec2 vTexcoord;
out vec4 vClipspace;

void main()
{
	gl_Position = aPosition;
	vClipspace = uProjMat * uViewMat * uModelMat * aPosition;
	vTexcoord = aPosition.xy * 0.5 + 0.5;
}